---
name: CI version matrices
status: open
created: 2026-06-18T15:49:59Z
updated: 2026-06-18T15:49:59Z
github: (will be set on sync)
depends_on: [001]
parallel: false
conflicts_with: [001]
---

# Task: CI version matrices

## Description

Validate the versions the README claims: run the build/test job across Node
`18/20/22`, and run the React hook tests across React `17/18/19`. Closes the gap
where 0.1.0 only tested Node 20 + React 19.

## Acceptance Criteria

- [ ] `ci.yml` build job runs a Node `[18, 20, 22]` matrix; all green.
- [ ] A React `[17, 18, 19]` matrix job installs the target React (+ react-dom +
      matching `@testing-library/react`) and runs the `src/react` tests; all green.
- [ ] The hook works unchanged on all three React versions (no code change expected —
      `useMemo`-only surface).
- [ ] README's "React 17/18/19" claim is now CI-backed (remove the backlog caveat).

## Technical Details

- Use `strategy.matrix` for Node. For React, either a separate matrixed job that
  `npm install react@$V react-dom@$V` before `vitest run src/react`, or a documented
  override. Keep `@testing-library/react` compatible with each React major.
- Remove the "cross-version React test matrix" item from SPLIT-PLAN §6 (backlog).

## Dependencies

- 001 (shares `ci.yml`; land after to avoid conflicts).

## Effort Estimate

- Size: S
- Hours: 2

## Definition of Done

- [ ] Code implemented
- [ ] CI green across all Node + React matrix cells
- [ ] Code reviewed
