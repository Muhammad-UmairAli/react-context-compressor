---
name: Edge-case test backfill
status: open
created: 2026-06-18T15:49:59Z
updated: 2026-06-18T15:49:59Z
github: (will be set on sync)
depends_on: [003]
parallel: false
conflicts_with: [003]
---

# Task: Edge-case test backfill

## Description

Backfill the edge-case tests deferred to SPLIT-PLAN §6 (backlog), pinning behavior
that currently works-but-is-untested so future refactors can't silently regress it.

## Acceptance Criteria

- [ ] Tests for null-prototype objects (`Object.create(null)`) — walk correctly to a
      plain object.
- [ ] Tests for non-plain built-ins: `RegExp`, `Error`, `TypedArray` — assert the
      documented/observed output shape (and update docs if a shape is changed).
- [ ] Test for `maxArrayLength: 0` — pin the documented behavior.
- [ ] Test for `dropEmpty` × truncation/`[Circular]` markers (markers are non-empty,
      so kept) — locks the ordering.
- [ ] Coverage ≥ 90% maintained; no production code change unless a test reveals a bug
      (if so, fix + note it).

## Technical Details

- Add to `src/core/compress.test.ts` (and `sanitize.test.ts` if relevant). If the
  RegExp/Error/TypedArray shapes are deemed surprising, decide on explicit handling
  here (e.g. `String(regexp)`, `{name,message}` for Error) and update the README.

## Dependencies

- 003 (shares `src/core`; lands after the robustness changes so tests cover final behavior).

## Effort Estimate

- Size: S
- Hours: 2

## Definition of Done

- [ ] Tests written and passing
- [ ] Coverage ≥ 90%
- [ ] Any doc updates for changed output shapes
- [ ] Code reviewed
